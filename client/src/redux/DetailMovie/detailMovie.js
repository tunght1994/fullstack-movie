import * as types from './type'


const initialState = {
    detail: {}
}


const detailReducer = (state = initialState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }

}

export default detailReducer